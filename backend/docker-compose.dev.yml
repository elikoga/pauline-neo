version: '3'
services:
  db:
    image: postgres:14.1
    environment:
      - POSTGRES_USER=pauline
      - POSTGRES_PASSWORD=pauline
      - POSTGRES_DB=pauline
    ports:
      - 5432:5432
    volumes:
      - ./db-data:/var/lib/postgresql/data
